```uml
@startuml
ユーザー -> webサーバー : 検索(商品名)
webサーバー -> DBサーバー : 検索処理(商品名)
DBサーバー -> DBサーバー : 検索処理
DBサーバー -> webサーバー : 検索結果

alt 検索一致
 webサーバー -> ユーザー : 検索結果を表示
else 検索不一致
 webサーバー -> ユーザー : 検索失敗メッセージを表示
 
 end
 
@enduml
```
